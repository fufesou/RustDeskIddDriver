name: build sign off

on:
  schedule:
    # schedule build every night
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  run-flutter-nightly-build:
    uses: ./.github/workflows/msbuild.yml
    secrets: inherit
    with:
      sign-mode: "off"
